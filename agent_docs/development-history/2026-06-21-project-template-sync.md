# Project Template Sync

- Applied the current `2030ai/project_template` baseline for agent-friendly repository structure.
- Added missing agent workspace mirrors, shared docs scaffolding, markdownlint config, and CI wiring where absent.
- Preserved project-specific docs and local instructions instead of overwriting them with template boilerplate.
