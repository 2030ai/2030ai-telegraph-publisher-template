# Markdownlint Scope

- Kept the markdownlint workflow scoped to the template-controlled docs synchronized in this PR.
- This preserves CI signal for the new baseline without forcing historical or generated Markdown into the same cleanup.
- The exact upstream template workflow remains intentionally different until legacy Markdown is cleaned up separately.
